# Dance tag audit exports

- `dance-tags-audit.csv` is committed as a text-based Excel-compatible audit file so PRs can show diffs and comments.
- `dance-tags-audit.xlsx` is generated by `npm run export:dance-tags` for local editing in Excel, but it is ignored by Git because `.xlsx` files are binary ZIP files that PR/chat diff viewers cannot render.
